<script id="comboItemTemp" type="text/template">
    <a href="{{Path}}" class="theme-selector-link">{{DisplayText}}</a>
</script>

@(Html.C1().ComboBox<CloudFileExplorer.Models.Theme>().CssClass("theme wide-screen").Bind(CloudFileExplorer.Models.Theme.GetAll())
    .IsEditable(false)
    .DisplayMemberPath("DisplayText")
    .ItemTemplateId("comboItemTemp")
    .SelectedIndex(CloudFileExplorer.Models.Theme.GetCurrentThemeIndex()))